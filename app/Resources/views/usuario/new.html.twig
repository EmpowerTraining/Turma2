{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cadastro de Usuário</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Cadastrar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('usuario_index') }}">Voltar a lista</a>
        </li>
    </ul>
{% endblock %}
